import type { SearchState } from '../SearchState/SearchState.ts'

export const handleInputCopy = async (state: SearchState, name: string): Promise<SearchState> => {
  // TODO write selected text to clipboard
  return state
}
