import type { SearchState } from '../SearchState/SearchState.ts'

export const renderSelections = (oldState: SearchState, newState: SearchState): readonly any[] => {
  // const { selections, uid } = newState

  // TODO
  return []
}
