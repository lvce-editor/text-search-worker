import { RendererWorker } from '@lvce-editor/rpc-registry'

export const { invoke, invokeAndTransfer, set, dispose, confirm, showContextMenu, writeClipBoardText, getFileIcon, openUri } = RendererWorker
