import type { SearchState } from '../SearchState/SearchState.ts'

export const handleInputContextMenu = async (state: SearchState): Promise<SearchState> => {
  // TODO show menu with cut/copy/paste
  return state
}
