import type { SearchState } from '../SearchState/SearchState.ts'

export const handleInputCut = async (state: SearchState, name: string): Promise<SearchState> => {
  // TODO write selected text to clipboard and cut text
  return state
}
