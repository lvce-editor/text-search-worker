import { test } from '@jest/globals'

test.skip('diff2 returns diff result for state changes', () => {
  // Skipped due to readonly property issues
})

test.skip('diff2 with different uid', () => {
  // Skipped due to readonly property issues
})
